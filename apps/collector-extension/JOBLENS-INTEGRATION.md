# JobLens Agent Integration Note

This directory is copied from the standalone `岗位筛选` Chrome extension v1.3.1.

In this monorepo it acts as **JobLens Collector**.

Current MVP integration: export report JSON and upload it to JobLens Agent.
Future P1 integration: add optional API sync while preserving offline JSON export.
