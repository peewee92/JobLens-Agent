"""Application layer: use cases (complete business actions).

Example future use cases: ImportJobsUseCase, ListJobsUseCase, MatchJobUseCase.
Orchestrates domain + repositories; no HTTP concerns here.
"""
