# Job Requirement Extraction API Contract

Status: Phase 3A frozen slice

## Goal

Convert one persisted Job description into an immutable, evidence-grounded JobRequirement Extraction Run. The result becomes the only JD fact source for later Eligibility / Match / Gap / Prepare workflows.

## Endpoints

### Create a new extraction

```http
POST /api/v1/jobs/{jobId}/requirement-extractions
```

The request body is empty. Backend reads the persisted Job description, invokes the configured Requirement Extractor, validates every evidence span, writes a Trace, then persists one immutable Extraction Run and its Requirements.

Successful response:

```http
201 Created
```

Body follows `packages/contracts/schemas/job-requirement-extraction.schema.json`.

### Read the latest extraction

```http
GET /api/v1/jobs/{jobId}/requirements
```

Returns the latest successfully persisted Extraction Run for the Job.

### Read a historical extraction

```http
GET /api/v1/jobs/{jobId}/requirement-extractions/{extractionId}
```

Returns one immutable historical Extraction Run. `extractionId` must belong to `jobId`.

## Stable errors

| HTTP | code | Meaning |
|---|---|---|
| 404 | `job_not_found` | Job does not exist |
| 404 | `job_requirement_extraction_not_found` | No latest extraction, or historical extraction not found for the Job |
| 422 | `job_description_not_extractable` | Job description missing/outside boundaries, or Collector v1.4.x marked it `card_only / partial_jd / unavailable` |
| 502 | `requirement_extractor_failed` | Provider invocation or response parsing failed |
| 502 | `invalid_requirement_extractor_output` | Structured output violated deterministic grounding/business rules |
| 503 | `requirement_extractor_unavailable` | Provider is disabled/not configured, or a transient upstream outage returned 429/503/504 |

## Deterministic validation

A provider result is rejected unless:

- at least one Requirement exists;
- each `originalText` and `evidenceSpan` is non-blank;
- Collector v1.4.x inputs must have `descriptionQuality=full_jd` and `requirementReviewEligible=true` before any model call or Trace is created;
- each `evidenceSpan` occurs as one contiguous substring of the persisted Job description;
- `originalText` also occurs in the description;
- confidence is in `[0, 1]`;
- skill requirements include a non-blank `normalizedCapability`;
- duplicate `(type, normalizedCapability, originalText, evidenceSpan)` entries are rejected; distinct Requirements may share a section-level `evidenceSpan` when their grounded `originalText` differs;
- all public enum values match the frozen Contract.

Structured Output guarantees shape only. It does not replace these checks.

## Versioning and persistence

Every successful extraction creates a new immutable Run, even when the Job and input hash are unchanged. This preserves model/prompt/extractor history. A Run records:

- Job ID;
- SHA-256 of the description;
- provider and model;
- extractor and prompt versions;
- Trace run ID;
- Requirement count;
- creation time.

Old Runs remain queryable. `GET /jobs/{jobId}/requirements` selects the latest Run by `createdAt DESC, extractionId ASC`.

## Trace and privacy

Trace capability: `requirement_extraction`.

Trace input refs contain:

```json
{
  "jobId": "job_xxx",
  "descriptionSha256": "...",
  "characterCount": 1234
}
```

Trace does not duplicate the full JD. Trace output may contain the structured Requirement proposal and therefore remains sensitive product data.

For deterministic quote recovery, Trace output also records audit-only grounding metadata:

```json
{
  "groundingPolicyVersion": "grounding-v1",
  "groundingRepairs": [
    {
      "requirementIndex": 0,
      "field": "originalText",
      "strategy": "punctuation_width"
    }
  ]
}
```

`grounding-v1` is deliberately narrow. Exact JD substrings remain preferred. Recovery may only ignore Unicode whitespace and normalize the width of ASCII punctuation, and only when the normalized quote maps to exactly one location in the JD. The value written to Trace and persistence is always the corresponding raw JD slice. Ambiguous matches, case changes, number/word rewrites, fuzzy matching and semantic rewrites remain invalid. `groundingRepairs` is diagnostic metadata only and does not relax the final verbatim validation gate.

After grounding, `requirement-extractor-v42.28` applies `requirement-semantics-v42.27`, the existing narrow deterministic semantic guardrail, before persistence. Trace records it separately:

```json
{
  "semanticPolicyVersion": "requirement-semantics-v42.27",
  "semanticRepairs": [
    {"requirementIndex": 0, "strategy": "waiver_scope"},
    {"requirementIndex": 2, "strategy": "split_trailing_preferred"},
    {"requirementIndex": 4, "strategy": "synthesize_alternative_group"},
    {"requirementIndex": 4, "strategy": "alternative_child"},
    {"requirementIndex": 7, "strategy": "inline_alternative_group"},
    {"requirementIndex": 8, "strategy": "split_hard_alternative"},
    {"requirementIndex": 9, "strategy": "split_education_experience"},
    {"requirementIndex": 10, "strategy": "split_experience_alternative"},
    {"requirementIndex": 11, "strategy": "split_skill_experience"},
    {"requirementIndex": 12, "strategy": "compound_hard_skill_constraint"},
    {"requirementIndex": 12, "strategy": "abstract_evaluative_skill_constraint"},
    {"requirementIndex": 13, "strategy": "drop_redundant_repaired_requirement"},
    {"requirementIndex": 14, "strategy": "recover_alternative_group_header"},
    {"requirementIndex": 15, "strategy": "drop_exact_duplicate_requirement"},
    {"requirementIndex": 16, "strategy": "expand_compound_hard_evidence_span"},
    {"requirementIndex": 17, "strategy": "normalize_experience_type_drift"},
    {"requirementIndex": 18, "strategy": "drop_example_child"},
    {"requirementIndex": 19, "strategy": "requirement_section_default_must_have"},
    {"requirementIndex": 20, "strategy": "abstract_evaluative_type_constraint"},
    {"requirementIndex": 21, "strategy": "split_embedded_preferred_with_covered_tail"},
    {"requirementIndex": 22, "strategy": "collapse_example_skill_siblings"},
    {"requirementIndex": 23, "strategy": "promote_hard_experience_prefix_before_soft_sibling"},
    {"requirementIndex": 24, "strategy": "drop_redundant_compound_hard_child"},
    {"requirementIndex": 25, "strategy": "conceptual_mechanism_skill_constraint"},
    {"requirementIndex": 26, "strategy": "responsibility_type_normalization"},
    {"requirementIndex": 27, "strategy": "non_experience_qualification_constraint"},
    {"requirementIndex": 28, "strategy": "drop_redundant_cross_type_duplicate"},
    {"requirementIndex": 29, "strategy": "preserve_conjunctive_hard_sibling"},
    {"requirementIndex": 30, "strategy": "drop_redundant_split_hard_parent"},
    {"requirementIndex": 31, "strategy": "collapse_compound_domain_siblings"},
    {"requirementIndex": 32, "strategy": "short_scope_summary_responsibility"},
    {"requirementIndex": 33, "strategy": "explicit_soft_marker_preferred"},
    {"requirementIndex": 34, "strategy": "recover_uncovered_cardinality_requirement"},
    {"requirementIndex": 35, "strategy": "drop_redundant_qualification_responsibility_child"},
    {"requirementIndex": 36, "strategy": "drop_redundant_qualification_subclause"},
    {"requirementIndex": 37, "strategy": "drop_redundant_recovered_cardinality_child"},
    {"requirementIndex": 38, "strategy": "normalize_hard_skill_capability_before_soft_suffix"},
    {"requirementIndex": 39, "strategy": "promote_hard_skill_prefix_before_soft_sibling"},
    {"requirementIndex": 40, "strategy": "drop_redundant_cardinality_fragment"},
    {"requirementIndex": 41, "strategy": "requirement_section_qualification_type_normalization"},
    {"requirementIndex": 42, "strategy": "split_hard_preferred_hard_mixed_scope"},
    {"requirementIndex": 43, "strategy": "collapse_compound_requirement_line"},
    {"requirementIndex": 44, "strategy": "collapse_preferred_alternative_group"},
    {"requirementIndex": 45, "strategy": "recover_alternative_group_child"},
    {"requirementIndex": 46, "strategy": "normalize_alternative_group_child_capability"},
    {"requirementIndex": 47, "strategy": "normalize_alternative_group_child_scope"},
    {"requirementIndex": 48, "strategy": "normalize_umbrella_skill_example_capability"},
    {"requirementIndex": 49, "strategy": "collapse_alternative_group_child_siblings"},
    {"requirementIndex": 50, "strategy": "recover_missing_alternative_group_parent"}
  ]
}
```

`requirement-semantics-v29` keeps all v28 protections and adds bounded evaluator-safety repairs from the frozen v38 Canary. Exact-grounded action sentences that are clearly job duties are normalized from Provider `skill`/`experience` drift to `responsibility`; an explicit responsibility/description section is preferred, with a heading-less fallback limited to long action sentences beginning with bounded duty verbs and excluding experience, education, threshold and soft-marker shapes. The hard-skill-plus-cardinality splitter now also accepts a Provider `constraint`: a prefix such as `熟练使用 Python` is recovered as a `skill must_have` only when the exact prefix matches the existing hard-skill grammar and the remaining exact clause contains an explicit at-least-one/alternative group; the normalized capability is derived only by stripping that bounded hard-skill verb prefix. A `must_have experience` that contains no experience/year wording may be normalized to `constraint` only when it exactly matches the narrow `具备...能力,能...` qualification shape. Finally, evaluator-risky cross-type duplicates are removed only when `originalText`, `evidenceSpan` and importance are identical and the verbatim text itself identifies the canonical type (`有/具备...经验` => experience, explicit hard-skill prefix => skill). Existing source-candidate, conceptual-mechanism, mixed education+experience, explicit-section importance, preferred-tail, example-sibling, numbered experience, compound-child dedupe, engineering-habit and alternative-group protections remain unchanged. Ambiguous/fuzzy source recovery is still forbidden. Grounding remains `grounding-v1`.

`requirement-semantics-v30` keeps all v29 protections and adds three exact-grounded evaluator-safety repairs from the frozen v40 Canary. First, an item that starts with an explicit conjunction such as `并/且/同时/以及`, shares the exact same evidence span with a preceding must-have cardinality constraint, immediately follows that constraint within the raw evidence, and contains no soft marker is preserved as `must_have` instead of being incorrectly downgraded as an alternative child. Second, a broad must-have constraint shaped as `hard skill, cardinality constraint` is dropped only when the exact hard-skill child and exact cardinality child are both already present as must-have Requirements; missing either child keeps the parent. Third, multiple `domain must_have` siblings are collapsed to one `constraint must_have` only when their `originalText` and `evidenceSpan` are identical, they carry distinct normalized capabilities, and the exact text is a compound `core technology list + execution/action` requirement. No fuzzy matching, semantic source recovery, Prompt change, coverage relaxation, or grounding relaxation is introduced.

`requirement-semantics-v31` keeps all v30 protections and adds one narrow repair from the frozen v41 Canary. A Provider-emitted `skill must_have` may be normalized to `responsibility` only when its exact evidence is a short top-level numbered summary line, the extracted text has no explicit qualification/skill/experience/education/threshold marker, the summary is followed within a bounded window by at least three clear action-shaped duty lines, and the same Provider output already contains at least three later `responsibility` Requirements grounded in that duty block. This prevents a role-scope summary such as `企业级AI与智能体应用开发` from becoming an exact normalized-skill hard gate while leaving explicit qualifications such as `熟练使用 Python` unchanged. Prompt v7, `requirement-coverage-v1`, and `grounding-v1` remain unchanged.

`requirement-semantics-v32` is the v42.1 patch from the stopped v42 Canary. It treats blank or whitespace-only `normalizedCapability` on non-skill Requirements as absent and keeps the mandatory alternative-group type repair eligible in that state. This closes the Harness regression where `不要求 ... 但至少要能独立 owner 一个核心方向,并能读懂、协作另一个方向` was persisted as `responsibility must_have` only because the Provider returned `normalizedCapability: ""`; the same exact-grounded mandatory qualification is now normalized to `constraint must_have`. The live adapter also canonicalizes blank non-skill capabilities to `null` before semantic repair. No grounding, Prompt, importance, cardinality, or coverage rule is relaxed.

`requirement-semantics-v33` is the v42.2 patch from the stopped v42.1 Canary. It adds three bounded repairs backed by exact JD evidence. First, a standalone clause already recognized as `有...经验者优先` or `熟悉...者优先` cannot remain `must_have`; it is normalized to `preferred` and duplicate copies collapse through the existing exact-dedupe stage. Mixed hard+soft clauses still use the existing split logic and are not softened wholesale. Second, an omitted Harness-style hard qualification is recovered only when an explicit requirements-section line starts with `不要求`, contains an all-of marker such as `都`, then an explicit `但至少...一个/一项/一种...` cardinality clause, has no soft marker, and is uniquely grounded; generic `至少一种/至少一个` lines are deliberately not synthesized. Third, a `responsibility must_have` sub-clause inside an explicit requirements section is dropped only when it is a strict exact substring of an already-emitted `constraint must_have` from the same raw source line. This removes duplicate qualification children without touching standalone job duties. Prompt v7, `requirement-coverage-v1`, and `grounding-v1` remain unchanged.

`requirement-semantics-v34` is the v42.3 patch from the stopped v42.2 Canary. It extends the existing abstract-evaluative type repair to Provider `experience` drift only when the exact text itself matches the bounded evaluative-ability grammar, so `工程能力扎实` becomes `constraint must_have` without weakening real experience facts. Harness-style `不要求...都...但至少...` lines are considered fully covered only by a full exact constraint; partial cardinality and conjunctive children no longer block full-line recovery and are removed once that exact full parent has been recovered. A hard skill `normalizedCapability` is reset to the exact capability captured from its hard-skill prefix only when the same unique source line contains a recognized trailing preferred clause, preventing soft experience semantics from leaking into a must-have capability. Finally, a `domain must_have` fragment is dropped only when it is a strict exact sub-clause of a same-evidence full `constraint must_have` inside an explicit requirements section. No fuzzy grounding, broad cardinality synthesis, or Prompt relaxation is introduced.

`requirement-semantics-v35` is the v42.4 patch from the stopped v42.3 Canary. Final duplicate identity now ignores only terminal punctuation (`。.;；,，`) on `originalText` and `evidenceSpan`, while still requiring the same type, importance and normalized capability. This removes punctuation-equivalent hard duplicates such as the same qualification emitted once with a trailing semicolon and once without it, without merging distinct capabilities, mixed importance, or semantically similar wording. Grounding, Prompt, cardinality and coverage behavior remain unchanged.

`requirement-semantics-v36` is the v42.5 patch from the stopped v42.4 Canary. It adds three evaluator-safety repairs without relaxing grounding. First, an evaluative qualification shaped as `优秀/良好...能力, 能...` that drifts to `responsibility` or `domain` is normalized to `constraint`; an identical existing constraint then collapses through final duplicate handling. Second, the existing hard-skill + product-experience splitter now classifies the skill prefix after removing only top-level numbering, so `5.熟练使用 ... , 对 ... 有高强度使用经验` still yields the hard tool-usage gate plus an independent `experience must_have`. Third, a Provider `experience must_have` with no experience/year wording may also normalize to `constraint` when it is an exact compound qualification whose first segment is an explicit hard-skill clause and whose remaining comma segments are bounded hard ability/action clauses. Real experience wording, soft markers, fuzzy text and semantic guessing remain excluded. Coverage remains `requirement-coverage-v2` and Prompt remains v7.

`requirement-semantics-v37` is the v42.6 patch from the stopped v42.5 Canary. It adds four narrow evaluator-safety protections. A hard-skill prefix such as `熟悉...` is promoted back to `must_have` only when the same uniquely grounded source line contains an independently emitted explicit preferred sibling and no `或/or` bridge makes the whole clause soft. A responsibility sub-clause may be dropped without an explicit section heading only when it is a strict substring of the same uniquely grounded evaluative qualification (`优秀/良好...能力, 能...`) already represented by a hard constraint. A standalone cardinality marker such as `至少` is removed only when the same evidence span already contains a longer must-have alternative/cardinality constraint. Finally, duplicate identity ignores presentation-only top-level numbering and terminal punctuation when the resulting verbatim requirement text maps to one unique source span; cross-type duplicates still resolve only to canonical explicit experience/skill types, and ambiguous repeated text is not merged. Grounding stays exact, coverage remains `requirement-coverage-v2`, and Prompt remains v7.

`requirement-semantics-v38` is the v42.7 patch from the stopped v42.6 Canary. It adds two system-level protections while preserving the ordering of earlier specialized repairs. After compound-domain collapse and qualification-subclause cleanup, a remaining `responsibility must_have` in an explicit requirements/qualifications section is normalized to `constraint` only when its exact text starts with a bounded qualification marker and contains no duty-action start, experience, education, threshold, or soft wording. A remaining `domain must_have` is normalized only when the exact domain clause also contains a later explicit ability/action segment; a plain domain fact remains `domain`. Separately, a fused `experience must_have` shaped as exact hard experience + explicit preferred clause + exact hard experience tail is split into three grounded Requirements only when both hard sides satisfy existing experience grammar; if the tail is already emitted as another hard Requirement, the older covered-tail splitter keeps precedence. This closes the observed hard/soft/hard scope leak without fuzzy recovery, broad section coercion, or semantic guessing. Coverage remains `requirement-coverage-v2` and Prompt remains v7.

`requirement-semantics-v39` is the v42.8 patch from the stopped v42.7 Canary. Section-heading detection now accepts the same supported requirement/responsibility heading names when wrapped by Chinese corner brackets such as `【任职要求】`, while preserving the normalized inner heading name for downstream section checks. In addition, an exact-grounded qualification line inside an explicit requirements section that matches the narrow `不要求...都...但至少...` mandatory-cardinality shape is normalized to `constraint must_have` even when the Provider emits it as `preferred` or as a non-constraint type. The repair still requires the existing alternative/cardinality grammar and forbids explicit soft markers; it does not generalize to arbitrary `preferred` text or fuzzy section inference. Coverage remains `requirement-coverage-v2` and Prompt remains v7.

`requirement-semantics-v40` is the v42.9 patch from the stopped v42.8 Canary. It addresses Provider fragmentation inside one explicit requirements/qualifications source line without introducing cross-line semantic merging. When a uniquely grounded requirement-section line contains at least two independently emitted hard fragments and the whole line is a bounded compound qualification with no education, experience, threshold, alternative-cardinality, soft marker, or inline-example list, the fragments collapse into one exact-grounded `constraint must_have`. Separately, when one uniquely grounded line ends in an explicit preferred marker and contains an `或/or` alternative relation, at least two non-hard preferred/bonus fragments collapse into one exact-grounded `constraint preferred`, preserving the original alternative group instead of allowing multiple independent preferred weights. Lines containing inline examples such as `(...如...)` are explicitly excluded so existing example-child extraction remains authoritative. Grounding stays exact, no fuzzy recovery is added, coverage remains `requirement-coverage-v2`, and Prompt remains v7.

`requirement-semantics-v41` is the v42.11 patch from the stopped v42.10 Canary. It restores omitted child options only for an already-emitted hard cardinality parent in an explicit requirements section when the exact parent announces a following list, ends with a colon, and the entire bounded scope before the next numbered sibling consists only of explicit direction/option labels or bullet children. Recovered children are exact JD slices, remain non-blocking `preferred`, and only expose a simple normalized capability when the child begins with one unambiguous capability token such as React/Electron/Python/Git. Mixed narrative scopes, soft/hard child markers, ambiguous grounding, fewer than two children, and oversized groups are rejected instead of guessed. Grounding stays exact and Prompt remains v7.

`requirement-semantics-v42` is the v42.14 patch from the stopped v42.13 Canary. It keeps the v41 alternative-child recovery and adds one bounded capability-scope repair for already-emitted `skill preferred` children inside exactly one explicit alternative-group scope. When the evidence line uses a direction/option label such as `【前端方向】`, the Provider capability is only the generic label text such as `前端`, and the child body starts with one unambiguous concrete capability token, the normalized capability is replaced with that concrete token (`React`, `Electron`, `Python`, `Git`). Provider-selected concrete capabilities such as `TypeScript` are preserved. The repair does not alter importance, source text, grounding, Prompt, cardinality, or retry policy.

`requirement-semantics-v42.15` is the v42.15 patch from the stopped v42.14 Canary. It addresses the live drift where the Provider shortened the hard parent `originalText` to `工程能力扎实` while keeping the full cardinality line in the exact `evidenceSpan`, causing the bounded alternative scope to disappear and four direction children to remain `responsibility must_have`. Scope recovery now falls back only to the exact parent evidence when the item is a hard constraint in an explicit requirements section, the evidence contains the existing alternative-cardinality and following-list grammar, and the line ends with a colon. Inside exactly one recovered scope, a supported direction/option child that drifted to `responsibility` or hard `skill` is normalized to `skill preferred`, and a simple first concrete capability token is used only when the current capability is blank or comes from the generic label. Existing `skill preferred` children continue through the v42.14 capability-only repair, preserving repair attribution and concrete Provider choices.

`requirement-semantics-v42.16` is the v42.16 patch from the stopped v42.15 Canary. It prevents a hard umbrella skill from being narrowed to one illustrative example. The repair applies only to an exact-grounded `skill must_have` whose original text contains a supported inline example marker (`如/例如/比如/such as/e.g.`), whose Provider-selected capability occurs only inside the example suffix, and whose prefix still matches the existing hard-skill grammar. The replacement capability is derived only by removing the bounded hard-skill verb from the exact umbrella prefix. `originalText`, `evidenceSpan`, `type`, and `importance` stay unchanged; a Provider-selected umbrella capability, preferred item, or concrete skill outside the example suffix is preserved.

`requirement-semantics-v42.17` is the v42.17 patch from the bounded v42.16 resume. It prevents a compound abstract evaluative qualification from becoming a literal Skill hard gate. The repair applies only to an exact-grounded `skill must_have` with no soft or alternative marker when the source contains multiple qualification segments, the first segment has a bounded evaluative shape such as `具有较强/优秀/良好的...能力|素养|意识`, and the Provider-selected capability is contained in that evaluative segment. The Requirement is normalized to `constraint must_have` with `normalizedCapability=null`, reusing the existing abstract-evaluative repair attribution. Technical skills, preferred items, alternative groups, and non-grounded text are unchanged.

`requirement-semantics-v42.18` is the first fan-out patch from the stopped v42.17 Canary. It only handled siblings whose `originalText` already equaled the full direction line. The live v42.18 Canary proved that assumption too narrow: the Provider emitted atomic `originalText` values such as `React` and `TypeScript` while keeping the complete `【前端方向】:...` line in each exact `evidenceSpan`, so fan-out remained.

`requirement-semantics-v42.19` extends that same bounded repair to the observed live shape. Inside exactly one alternative-group scope, at least two `skill preferred` siblings must share the same exact direction-line `evidenceSpan`, carry distinct capabilities and distinct atomic `originalText` fragments, and every atomic fragment must occur verbatim inside that direction body. The siblings are then normalized to the complete direction line plus its first unambiguous concrete capability, after which existing exact duplicate handling keeps one non-blocking direction node. A single concrete child remains untouched; no cross-line, cross-scope, importance, grounding, cardinality, Prompt, JSON validation, or retry behavior changes.

`requirement-semantics-v42.20` closes the live v42.19 type-drift gap. The same direction-level collapse now accepts non-blocking `skill preferred` and `domain preferred` siblings, because the Provider may classify technical fragments from one direction line inconsistently across those two types. The evidence/group/atomic-fragment requirements remain unchanged; once the sibling group is proven, every member is normalized to one `skill preferred` direction node using the full exact direction line and its first concrete capability. Must-have items, other types, cross-line/cross-scope items, and single concrete children remain untouched. The live v42.20 Canary exposed one remaining implementation assumption: some `domain preferred` atoms carry `normalizedCapability=null`, so requiring every sibling to have a normalized capability excluded the real desktop/backend/engineering fan-out shape.

`requirement-semantics-v42.21` makes only that sibling proof capability-null-safe. Inside the same unique bounded alternative scope and exact shared direction evidence, a `skill preferred` or `domain preferred` atom is eligible when its non-empty `originalText` occurs verbatim in the direction body; proving at least two distinct atomic `originalText` siblings is sufficient even when a domain atom has no normalized capability. The canonical direction capability is still derived from the exact direction line's first unambiguous concrete token. This does not relax grounding, importance, cardinality, scope, Prompt, Provider JSON validation, or retry behavior, and a single concrete child remains untouched.

`requirement-semantics-v42.22` closes the translated generic-label gap observed in the stopped v42.21 Canary. Within the same exact direction-child grammar and bounded alternative scope, a small explicit alias table recognizes only supported translations of the four live direction labels (`Frontend`, `Desktop`, `Backend`, `Engineering/Code Control` and tightly equivalent spellings) as generic label capabilities. Those aliases may therefore normalize to the first exact concrete capability token on the direction line (`React`, `Electron`, `Python`, `Git`). Arbitrary semantic translations are not inferred, concrete Provider-selected capabilities such as `TypeScript` are still preserved, and scope/importance/grounding/Prompt/JSON/retry behavior is unchanged.

`requirement-semantics-v42.23` handles the stopped v42.22 Canary where the Provider omitted the explicit hard parent `至少在以下一个方向...:` while still emitting every following direction line as exact evidence. Recovery is allowed only for an exact line inside a recognized requirements section that contains the existing alternative/cardinality grammar, announces a following list, ends with a colon, has no soft marker, and is followed before the next numbered sibling by 2–8 pure direction-label children. Every child line must already be represented by Provider evidence before the parent is synthesized as `constraint must_have`; incomplete child coverage, mixed narrative scopes, explicit child hard/soft markers, ambiguous grounding, and unrelated labeled skills remain untouched. Once that parent restores the bounded scope, existing alternative-child and sibling-collapse repairs do the normalization; no new global softening rule is introduced.

`requirement-semantics-v42.24` closes the importance-drift gap exposed by the stopped v42.23 Canary. The existing direction sibling-collapse proof now also accepts `bonus` atoms, but only when at least two distinct atomic siblings share the same exact direction evidence inside one unique bounded alternative scope. Once proven, every participating sibling is normalized to one `skill preferred` direction node using the exact line's first concrete capability. A lone bonus, cross-direction or cross-scope items, explicit hard children, unrelated evidence, and non-direction lines are untouched. Parent cardinality, grounding, Prompt, Provider JSON validation, and retry behavior remain unchanged.

`requirement-semantics-v42.25` closes the standalone parenthetical example-child gap exposed by the stopped v42.24 Canary. The existing exact-evidence `drop_example_child` proof now also recognizes a child whose entire `originalText` is one bounded parenthetical example list ending in `等`/`etc` (for example `(代码助手、运维助手、文档问答等)`). It is removed only when the same exact evidence span contains an earlier non-example `must_have` parent Requirement. Parenthetical text without that shared hard parent, non-example parentheticals, cross-evidence items, and soft requirements are preserved. Grounding, Prompt, Provider JSON validation, retry policy, and responsibility importance rules are unchanged.

`requirement-semantics-v42.26` extends the existing explicit-experience normalization to Provider-native `experience must_have` output. When the exact text consists of exactly two comma-separated segments, the first is a bounded explicit experience fact and the second is only a recognized negative explanation/consequence (for example `需要有车端经验,非车端经验的无法到副总师的层级`), persistence keeps only the exact experience fact and clears normalized capability. A normal single experience fact is unchanged and does not create a repair event; a suffix that is itself another hard requirement is not stripped. Grounding, Prompt, Provider JSON validation, retry policy, and other type rules remain unchanged.

`requirement-semantics-v42.27` closes the full-line alternative-child fan-out exposed by the stopped v42.26 Canary. The existing bounded direction sibling proof already collapses multiple preferred/bonus atoms when their `originalText` values are distinct fragments from one exact direction line. v42.27 additionally accepts the Provider shape where every sibling repeats the same full direction line as both `originalText` and `evidenceSpan`, but assigns at least two distinct non-empty normalized capabilities. Only siblings inside one unique bounded alternative scope and the same exact direction evidence participate; the canonical capability still comes from the line's first concrete token, and a single full-line child, repeated identical capability, explicit hard child, cross-direction item, or unrelated evidence is preserved. Parent cardinality, grounding, Prompt, Provider JSON validation, and retry behavior remain unchanged.

`requirement-extractor-v42.28` changed only the Provider-facing JSON Schema, not Requirement semantics. The Provider saw one flat Requirement object with the common fields `type`, `originalText`, `sourceCandidateId`, `importance`, `confidence`, and nullable `normalizedCapability`, instead of the previous discriminated `oneOf` union. Live evidence did not improve malformed JSON reliability and also produced `skill + normalizedCapability=null`, so v42.29 restores the strict discriminated Provider schema while keeping `requirement-semantics-v42.27` unchanged. Model selection remains runtime configuration; v42.29 uses `deepseek-v4-pro` only as a controlled live isolation variable and does not hardcode that model in the contract.

`requirement-semantics-v42.30` closes the two semantic Bad Cases exposed by the stopped v42.29 Canary without changing grounding, importance or cardinality. Inside the existing unique bounded alternative-direction scope, the explicit alias table now also recognizes the Provider's semantic generic labels `frontend engineering`, `desktop application development`, `backend engineering`, and `engineering infrastructure`, so those non-blocking children normalize to the exact line's first concrete capability (`React`, `Electron`, `Python`, `Git`). Separately, when at least two `responsibility` items have the same exact `originalText`, exact `evidenceSpan`, and importance, but differ only by non-empty normalized capability, they are treated as duplicate annotations of one duty fact and collapse to one responsibility with `normalizedCapability=null` and the minimum sibling confidence. A single responsibility capability annotation, cross-evidence items, cross-type items and different importance remain untouched.

`requirement-semantics-v42.32` extends only the bounded alternative-direction sibling proof exposed by the stopped v42.31 Canary. A Provider item typed as `experience preferred/bonus` may participate in direction fan-out collapse only when its exact `originalText` is inside the same direction body and contains no explicit experience marker or threshold. Such drifted atoms are canonicalized with existing skill/domain siblings to one `skill preferred` direction node. True experience text such as `3年以上桌面端开发经验`, any `experience must_have`, cross-direction items, unrelated evidence and non-alternative scopes remain untouched.

`requirement-semantics-v42.33` adds explicit bonus-section importance inheritance for the live Case 9 regression discovered during the v42.32 bounded resume. When a requirement's exact evidence is located after a standalone `加分项`/`加分条件` (or explicit English bonus-heading equivalent) and before the next recognized section heading, Provider `must_have`/`preferred` importance is normalized to `bonus`. The proof is positional and exact-grounded; it does not infer softness from semantics, does not change type/capability/text, and does not leak across the next section heading. Existing inline `者优先` handling, alternative/cardinality semantics, grounding, coverage and Provider JSON validation remain unchanged.

`requirement-semantics-v42.34` extends the existing bounded alternative-direction sibling proof to Provider `constraint preferred/bonus` atoms after the stopped v42.33 Canary exposed that new type drift. Such a constraint may participate only when it shares the same exact direction evidence inside one unique alternative scope and at least one sibling proof establishes a real fan-out. `constraint must_have` is explicitly excluded, so true hard child constraints remain separate. Once proven, participating atoms canonicalize with existing skill/domain/experience siblings to one `skill preferred` direction node using the exact line's first concrete capability. Single constraints, cross-direction items, cross-scope items, true hard constraints, grounding, coverage and Provider JSON validation remain unchanged.

`requirement-semantics-v42.35` closes the presentation-only full-line gap exposed by the stopped v42.34 Canary. The existing full-line sibling proof may treat `originalText` as the same direction line as exact `evidenceSpan` when the two values are byte-for-byte identical after stripping only terminal punctuation (`，,。.;；`) and surrounding whitespace. It still requires one exact direction evidence inside one unique bounded alternative scope plus at least two distinct non-empty normalized capabilities. A single presentation-drifted child remains untouched; differences in wording, labels, internal punctuation, case, scope or evidence are not normalized. Once proven, the canonical persisted direction text is restored from exact evidence and the first concrete capability is retained.

`requirement-semantics-v42.36` closes a split-compound evaluative-skill regression observed during the v42.35 bounded resume. A `skill must_have` is converted to `constraint must_have` with `normalizedCapability=null` only when its exact `originalText` is the first comma-delimited segment of the same exact compound `evidenceSpan`, that segment matches the existing abstract evaluative ability grammar (`具有较强/优秀/良好/...能力|素养|意识`), and the evidence contains at least one additional clause. This covers Provider output such as `具有较强的沟通、表达、总结及文档制作能力` being isolated from the larger qualification line and labeled as a literal Skill hard gate. Standalone technical skills, soft items, alternative scopes, cross-evidence items and non-evaluative ability clauses remain untouched.

`requirement-semantics-v42.37` removes responsibility parent/subclause duplicate weighting only when the same exact evidence already contains a full `responsibility` parent whose `originalText` equals `evidenceSpan`, the child has the same importance, and the child's exact `originalText` is a strict substring of that parent. Without a full parent, distinct responsibility subclauses are preserved. Cross-evidence, cross-importance and non-responsibility items are untouched.

`requirement-semantics-v42.38` recovers a hard technical skill mislabelled by the Provider as `experience must_have`. Repair requires unique exact grounding, a single bounded hard technical-skill clause beginning with the existing `熟悉/熟练/掌握/精通/使用` grammar, and no experience/year, soft or alternative wording. Inline example lists are excluded from capability derivation so umbrella skills such as `智能体(Agent)技术栈` do not collapse to LangChain/AutoGPT. Real experience facts remain experience.

`requirement-semantics-v42.39` extends the existing umbrella example-capability repair to the Provider shape where `normalizedCapability` equals the complete hard-skill phrase including the parenthetical example list. Both example-only capability leakage and full-phrase example leakage canonicalize to the exact umbrella prefix only when the bounded hard-skill grammar is satisfied. Grounding, importance, cardinality, coverage and Provider JSON validation remain unchanged.

`requirement-semantics-v42.40` extends the existing abstract-evaluative hard-skill normalization to a bounded multi-clause trait shape observed in live Case 3. A `skill must_have` such as `具有高度的责任心,并具有较高的抗压能力` is converted to `constraint must_have` only when every comma segment matches the narrow responsibility/pressure trait grammar and the current normalized capability is contained in that exact text. Concrete technical compound skills remain skills. No importance, grounding, coverage, cardinality or Provider contract is changed.

`requirement-semantics-v42.41` closes the two Case 0 regressions exposed by the stopped v42.40 Canary without changing cardinality or Provider behavior. Exact duplicate identity ignores `normalizedCapability` only for `experience`, because capability is not the fact identity for a verbatim experience requirement; this allows two identical `需要有车端经验` hard facts to collapse even when the Provider decorates one with a capability, while `skill`, `domain`, responsibility and other capability-bearing siblings keep their previous identity rules. The umbrella example-capability repair also compares the Provider's full-phrase capability against the exact hard-skill phrase using the existing whitespace-removal and ASCII/full-width punctuation normalization before canonicalizing to the exact umbrella prefix. Wording changes, semantic aliases and fuzzy matching remain unsupported.

`requirement-semantics-v42.42` extends only the existing alternative/cardinality grammar to a bounded Chinese postfix form such as `C/C++/Go/Python等一种以上编程语言`. The new form requires `等` or `中`, followed within eight characters by `一门/一个/一项/一种` plus `以上`; it then reuses the established inline-cardinality normalization, so multiple Provider hard skills collapse to one `constraint must_have` instead of becoming independent hard gates. No generic `以上` rule, fuzzy parsing, importance relaxation or Provider contract change is introduced.

`requirement-semantics-v42.43` hardens the experience duplicate identity exposed by the stopped v42.42 live run. For `experience` only, duplicate identity now ignores source/evidence-span widening as well as Provider capability decoration when type, importance and presentation-equivalent `originalText` are identical. This covers repeated occurrences of the same experience sentence in one JD when the Provider anchors one row to a narrow evidence span and another to a broader containing span. Other requirement types keep source identity, so distinct skill/domain/responsibility facts are not merged.

`requirement-semantics-v42.44` adds a bounded responsibility-enumeration collapse for dense outputs that remain within the Provider's 50-item schema limit before semantic repair. When two or more `responsibility` rows share the exact same grounded evidence, that evidence explicitly contains `包括但不限于`, and every child `originalText` is a verbatim substring of that evidence, the rows are collapsed to one full-evidence responsibility with no capability annotation. This avoids duplicate weighting and leaves room for required deterministic cardinality parents without truncating facts or raising the 50-item safety limit. Responsibility siblings without the explicit enumeration marker remain independent.

`requirement-semantics-v42.45` hardens two live qualification semantics without changing Provider output contracts. Explicit requirement-section detection accepts a single bounded Chinese or Arabic ordinal prefix such as `二、任职要求` or `2. 任职要求`, so unsoftened qualification rows continue to default to `must_have`; explicit soft markers still block that promotion and alternative/cardinality rules may still downgrade true alternatives later. The abstract-evaluative hard-skill normalization also accepts a standalone exact responsibility/pressure trait already covered by the bounded trait grammar, such as `具有较高的抗压能力`, and converts it to `constraint must_have`. Concrete technical ability text remains a skill.

`requirement-semantics-v42.46` adds one narrow reverse type repair for a live false-accept risk: an exact `constraint must_have` in an explicit requirements section is restored to `skill must_have` only when it has a hard-skill verb, an inline example list, no soft marker, and the exact umbrella prefix ends in `技术栈`. The capability is derived verbatim from that umbrella after removing the bounded qualification verb, e.g. `熟悉智能体(Agent)技术栈(如LangChain、AutoGPT等)` becomes capability `智能体(Agent)技术栈`. This is not a generic constraint-to-skill conversion; conceptual mechanism, evaluative ability, and ordinary constraint text remain unchanged.

`requirement-semantics-v42.47` closes a live example-fragment hard-gate regression without weakening real qualifications. When an exact hard umbrella requirement and later fragments share the same exact evidence span, that evidence contains the explicit continuation marker `包括但不限于`, and the umbrella occurs before the marker, fragments at or after the marker are treated as redundant examples and dropped with the existing `drop_example_child` repair. This covers Provider fan-out such as `对RAG和agent框架有基本了解,包括但不限于 langChain、llama index、autoGen、metaGPT等` becoming one hard umbrella requirement rather than four additional hard framework gates. The rule does not fire when the umbrella parent is absent, when evidence differs, or when the child occurs before the marker.

`requirement-semantics-v42.48` extends that same exact-evidence example protection to a bounded non-parenthetical illustrative marker: `,如/例如/比如 ...`. A hard umbrella sibling must still occur before the marker in the same evidence; only later fragments are dropped, so a standalone child or a child anchored to different evidence is preserved. The existing umbrella-capability repair also recognizes this marker, preventing an example such as `Transformer` from replacing the true hard capability `大语言模型核心技术`. This specifically closes the live Case 6 shape `熟悉大语言模型核心技术,如 Transformer 架构、微调(如 LoRA、PEFT)、AI智能体等` without treating ordinary comma-separated hard requirements as examples.

`requirement-semantics-v42.49` extends only the existing non-experience qualification type repair. A Provider `experience must_have` may become `constraint must_have` when it contains no experience/year/soft wording, its first comma segment matches the already-bounded explicit core-technology list grammar (`理解/熟悉/掌握/精通/了解 ... 等核心技术`), and every later segment remains a recognized hard qualification segment such as `能独立...`. This recovers the live Case 2 clause `理解RAG、向量数据库、Function Calling等核心技术,能独立完成原型验证与方案评估` without converting ordinary experience statements or broad conceptual prose.

`requirement-semantics-v42.50` narrows duplicate identity only for an explicit soft preferred fact that the Provider fans out into several capability decorations. When `type`, `importance=preferred`, presentation-equivalent `originalText`, and source identity are the same, capability is ignored for duplicate identity only if the original/evidence itself contains an explicit soft marker such as `优先/preferred/bonus`; the first grounded row is retained and later copies use the existing `drop_exact_duplicate_requirement` repair. Unsoftened preferred multi-capability facts remain distinct, so a line such as `熟悉 Agent 和 RAG 相关技术` can still produce both Agent and RAG capabilities, and hard/bonus facts remain capability-sensitive. This closes the v42.49 live duplicate-weighting shape without making generic preferred deduplication lossy.

`requirement-semantics-v42.51` extends that duplicate protection only to a repeated preferred child inside one already-detected cardinality-group scope. If multiple rows have the same presentation-equivalent child text, evidence, type, and `preferred` importance, occur inside exactly one bounded alternative/cardinality scope, and differ only by capability decoration, capability is ignored for exact duplicate identity and one child node is retained. This covers the live Case 4 shape `模型微调(SFT / LoRA / QLoRA)` being emitted three times as `模型微调`, `LoRA`, and `QLoRA`: it remains one non-blocking direction under the hard `至少覆盖以下方向中的两项` parent. Preferred multi-capability facts outside a cardinality scope, hard facts, and separately grounded children remain capability-sensitive.

`requirement-semantics-v42.52` extends only explicit bonus-section heading recognition. The existing bonus inheritance now accepts one bounded Chinese or Arabic ordinal prefix and an optional short parenthetical annotation, for example `三、加分项(满足越多越优先)`. Items grounded below that heading inherit `bonus` exactly as they already do for plain `加分项:` headings, and inheritance still stops at the next recognized section boundary. This closes the live Case 4 false-reject where API gateway, multi-model orchestration, inference-engine and CUDA bonus items were emitted as `must_have`; ordinary requirement sections, inline soft markers and cardinality rules are unchanged.

`requirement-semantics-v42.53` narrows one remaining umbrella-capability leak. For an exact hard skill with an inline example list, if the Provider capability starts with the full hard phrase but appends a trailing fragment, the fragment is stripped only when that same fragment occurs immediately after the hard phrase in the JD and the following source window contains an explicit soft marker such as `优先`. This recovers `熟悉智能体(Agent)技术栈(如LangChain、AutoGPT等)` from a capability like `智能体(Agent)技术栈(如LangChain、AutoGPT等),有落地项目经验` back to the hard umbrella `智能体(Agent)技术栈` without truncating unrelated extended hard capabilities.

`requirement-semantics-v42.54` fixes one bounded cardinality-scope boundary defect. When an inline one-of/cardinality parent sits on a bare numbered JD line such as `1 精通 Python 或 Go 或 Java 至少一门语言...`, the scope boundary now derives the parent ordinal from the start of the containing source line even when text exists between that ordinal and the detected group phrase. The next ordinal sibling such as `2 熟悉后端组件`, `3 具备分布式经验`, or `4 熟练使用 Docker/Kubernetes` therefore terminates the one-of scope and remains an independent hard requirement. True multi-line cardinality children under the same numbered parent remain inside scope.

`requirement-semantics-v42.56` closes a same-evidence hard fan-out observed on the Harness engineering-practice clause. The repair only recognizes an exact numbered clause shaped as `有良好的工程习惯,重视...` whose quality list contains at least three items from the bounded set `可维护性 / 测试 / 可观测性 / 安全边界 / 长期演进成本`. If the Provider emits three or more of those qualities as separate hard rows sharing that exact evidence, they are collapsed into one `constraint must_have` using the full grounded clause and no capability annotation. Same-evidence technical hard siblings such as Python/Docker/Kubernetes are not affected.

`requirement-semantics-v42.57` closes a narrower duplicate shape on a hard umbrella immediately before an explicit `包括但不限于` example list. When two or more `must_have skill` rows have presentation-equivalent original text and evidence, the original occurs before the marker in that exact evidence, and the rows differ only by capability decoration, capability is ignored for exact-duplicate identity and only the first grounded umbrella row is retained. Hard multi-capability facts without that example marker remain capability-sensitive, so ordinary `Agent + RAG` requirements are not collapsed.

`requirement-semantics-v42.58` extends the existing conceptual-mechanism normalization to one additional Provider type drift. An exact `domain must_have` shaped as the already-supported umbrella mechanism clause (for example `理解 LLM / Agent 的基本机制,包括 ...`) is normalized to `constraint must_have` with no capability annotation, using the same strict grounding, hard-importance, no-soft-marker, and concept-list grammar as the existing skill repair. Concrete domain knowledge remains `domain`; this is not a generic domain-to-constraint conversion.

`requirement-semantics-v42.59` narrows one requirement-section importance drift for education facts. The existing `requirement_section_default_must_have` repair already promotes unsoftened preferred qualification-shaped items under an explicit requirements/qualifications heading, but it previously required an action-style prefix such as `本科/硕士/必须/需要/熟悉...`. v42.59 additionally accepts `education` rows whose uniquely grounded text contains an education signal (`学历/学位/本科/硕士/博士/大专/专科/专业`) even when the line begins with a modifier such as `知名高校`. The repair still requires the nearest section to be a requirements/qualifications section and rejects any original/evidence containing an explicit soft marker, so `知名高校本科及以上学历者优先` remains preferred and education facts outside requirement sections are unchanged.

`requirement-semantics-v42.60` narrows exact-duplicate identity for `education` facts. If the Provider emits the same presentation-equivalent education `originalText`, importance, and evidence more than once while decorating each copy with a different `normalizedCapability` (for example `计算机科学` and `人工智能` for the single source fact `计算机科学、人工智能等相关专业本科及以上学历`), capability is ignored for duplicate identity and only one grounded education requirement is retained. Distinct education source facts remain separate because their original/evidence identity still differs; this is not a generic education merge.

`requirement-semantics-v42.61` closes the remaining conceptual-mechanism `domain` type-drift shape observed in the v42.60 live Canary. The existing exact umbrella grammar still requires `must_have`, unique verbatim grounding, no soft/alternative marker, a full `理解/了解 ... 基本机制/工作机制, 包括 ...` shape, at least three enumerated concepts, and no action clause. For `domain` rows only, `normalizedCapability` may now be absent because the final repaired fact is a capability-less `constraint`; when it is absent, `originalText` itself must match the complete umbrella grammar and the repair will not recover a broader clause from `evidenceSpan`. `skill` rows still require a capability before this normalization can fire. This does not broaden generic `domain` handling.

`requirement-semantics-v42.62` normalizes source identity for one bounded alternative-direction child shape. If a row is already a `preferred skill` inside exactly one alternative scope, its `evidenceSpan` is a supported labeled direction line such as `【工程化/代码控制方向】:Git、...`, its `originalText` is exactly the unlabeled body, and its capability already equals the body's first concrete capability, the repair restores `originalText` to the full labeled evidence line. Importance, type, capability and cardinality semantics do not change. This keeps alternative-direction nodes source-identical whether the Provider emitted the full line or only the label body; unrelated preferred skills and ambiguous scopes are unchanged.

`requirement-semantics-v42.63` keeps the same v42.62 scope but makes only the body/source comparison presentation-equivalent. The v42.62 live Canary showed that the Provider strips the terminal `。` from body-only `originalText` while the labeled `evidenceSpan` retains it, so strict `originalText == body` prevented the repair from firing. v42.63 ignores only terminal presentation punctuation for that equality check; the row must still be a `preferred skill`, belong to exactly one bounded alternative scope, use the supported labeled evidence grammar, and already carry the body's first concrete capability. Importance, type, capability and cardinality remain unchanged.

`requirement-semantics-v42.64` extends the existing same-source hard example-capability collapse to a second explicit example grammar: a parenthetical slash/Chinese-enumerated list ending in `等`/`etc.`, such as `熟悉主流大模型(GPT 系列 / DeepSeek / Llama / Qwen 等)的技术特性和使用场景`. If the Provider emits two or more `must_have skill` rows with the same exact grounded original/evidence and only different capabilities, the rows collapse to one `constraint must_have`. Parenthetical capability lists without an example marker (`等`/`etc.`) remain capability-sensitive, and importance/type/source grounding requirements are unchanged.

`requirement-semantics-v42.65` closes one exact-source experience fan-out shape observed in the v42.64 bounded live segment. When a uniquely grounded requirement-section clause matches the narrow form `有/具备 ... 经验, 能/能够 ...`, contains no soft marker, and the Provider already emitted an exact same-original/same-evidence `experience must_have`, that hard experience becomes the canonical fact. Same-source copies of the complete clause emitted as `constraint must_have` or downgraded `preferred skill` rows are dropped as evaluator-risky duplicates. This does not merge different originals from one line, does not run outside an explicit requirements section, and does not run when the source contains an explicit soft marker.

`requirement-semantics-v42.66` repairs one Harness-only source-shape failure without introducing generic list merging. If an explicit numbered requirement line is a hard cardinality group header, contains a following-list marker such as `以下/下列`, ends with `:`/`：`, and the Provider splits that single line into multiple hard `constraint/responsibility` fragments, the fragments collapse back to the exact full source line as one `constraint must_have`. This restoration happens before alternative-child canonicalization, so labeled direction lines such as `【前端方向】:React、TypeScript...` regain a valid bounded alternative scope and existing sibling-dedupe logic can collapse atomic preferred fan-out back to one preferred direction node. Other cardinality requirement lines keep the older recovery path unchanged, and any source with education/experience/soft markers is excluded.

`requirement-semantics-v42.67` extends only the existing redundant-example-child guard to inline parenthetical example lists. If a hard atomic child shares exact evidence with an earlier hard umbrella and the child's exact grounded `originalText` lies fully inside a bounded `(如/例如/比如/such as/e.g. ... )` region, the child is dropped with the existing `drop_example_child` repair. This prevents examples such as `LangChain` and `AutoGPT` inside `熟悉智能体(Agent)技术栈(如LangChain、AutoGPT等)` from becoming independent hard gates while retaining the umbrella requirement. The rule does not remove content after the closing parenthesis, does not run without a same-evidence hard parent, and does not introduce generic parenthetical deduplication.

`requirement-semantics-v42.68` extends only the existing exact-source recovery for non-list cardinality requirements. The recovery scanner now treats bracketed requirement headings such as `【任职要求】` the same as the already-supported plain `任职要求` heading. When the bounded `不要求...都...,但...至少...` source line is recovered as a full `constraint must_have`, same-evidence strict subfragments emitted as `skill must_have` are removed in addition to the already-supported constraint/responsibility fragments. The source must still be uniquely grounded, contain no soft marker, and satisfy the existing cardinality grammar; no new semantic reconstruction is introduced.

`requirement-extractor-v42.69` keeps `requirement-semantics-v42.68` and `requirement-coverage-v4` unchanged, but separates Provider raw structured-output capacity from the final business limit. The Provider schema may return up to 64 raw requirement rows so deterministic grounding/semantic repair can collapse redundant fan-out before final validation. Persisted/business output is still limited to 50 requirements by `validate_job_requirement_output`, and coverage gates remain fail-closed. This addresses live Case 4 evidence where the Provider returned exactly 50 raw rows (indices 0–49), consumed capacity on pre-repair fan-out, and omitted the trailing seven-item bonus section before coverage validation could evaluate it.

`requirement-semantics-v42.70` fixes one bounded bare-numbered cardinality scope leak exposed after v42.69 finally produced a complete Case 4 extraction. When the alternative/cardinality parent itself starts at the beginning of a source line with a bare ordinal such as `4 熟练使用 PyTorch 或 TensorFlow,熟悉 LangChain / LlamaIndex / Dify 等至少一种`, the scope boundary now derives that ordinal from the group text itself and terminates at the next contiguous bare-numbered sibling such as `5 有向量数据库...实际使用经验`. This prevents the independent sibling from being downgraded by `alternative_child`. Existing dotted-number boundaries, noncontiguous cardinality recovery, and true children within the group remain unchanged.

`requirement-semantics-v42.71` removes one duplicate-weighting shape exposed by the v42.70 Case 4 live output. If a uniquely grounded `constraint must_have` cardinality parent covers the full exact evidence line and a same-evidence `skill must_have` equals only the complete comma-delimited leading clause of that parent, the leading skill is dropped because the full parent already represents it. The repair requires the same evidence, an exact full parent, a real cardinality marker, and a complete leading clause; different-evidence skills and generic substrings are preserved.

`requirement-semantics-v42.72` fixes one bounded application-material importance drift exposed by v42.71 Case 5. A uniquely grounded `constraint` that the Provider marks `preferred`/`bonus` is promoted to `must_have` only when the source contains no soft marker and uses an explicit application imperative such as `投递/申请/应聘` together with `附带/附上/提供/提交` and a concrete portfolio-style material (`GitHub`, `作品集`, `portfolio`, personal/open-source page). Generic GitHub skills, open-source experience, and explicitly softened bonus statements remain soft.

`requirement-semantics-v42.73` closes two exact-source duplicate gaps exposed by the v42.72 Case 0 live output. Responsibility-subclause dedupe now accepts a full responsibility parent when its `originalText` is presentation-equivalent to the same exact evidence after stripping only a leading list ordinal and terminal punctuation; same-evidence strict hard responsibility children remain removable, while unrelated or different-evidence duties remain intact. Qualification-subclause dedupe also accepts an `education must_have` strict child already contained in a same-evidence `education must_have` parent inside a requirements section. No generic semantic substring dedupe is introduced.

`requirement-semantics-v42.74` extends only that existing same-source qualification-subclause dedupe to one Provider type-drift shape exposed by the v42.73 Case 0 live output. A strict `experience must_have` child may be removed under a same-evidence hard constraint only when the child text itself does not contain an explicit experience signal such as a years-of-experience threshold or an `有/具备...经验` fact. This removes a duplicate child like `具备企业级AI系统架构设计能力` that was incorrectly typed as experience while preserving genuine experience requirements even when they share a broader source constraint. Different evidence and non-substring facts remain untouched.

`requirement-semantics-v42.75` extends only the existing explicit-requirements-section hard-importance recovery to an experience threshold that the Provider incorrectly labels preferred. A grounded `experience preferred/bonus` row inside a recognized requirements/qualifications section may be promoted when its own text contains an explicit experience fact together with a hard threshold marker such as `3 年以上`; the row must still contain no explicit soft marker such as `优先/加分/preferred/bonus`. This covers countability qualifiers such as `3 年以上工作经验(研究生期间参与的实际项目经验可计入)`: the parenthetical changes which experience may count toward the threshold, not whether the threshold is mandatory. Explicitly softened experience thresholds remain preferred.

`requirement-semantics-v42.76` extends only the existing same-evidence leading-cardinality-subclause dedupe to a `constraint must_have` child produced after semantic normalization. The parent must still be a uniquely grounded hard cardinality constraint covering the same exact evidence, and the child must still equal only the complete comma-delimited leading clause of that parent. This closes the live shape where `熟练使用 PyTorch 或 TensorFlow` is normalized into a standalone hard constraint while the full `4 熟练使用 PyTorch 或 TensorFlow,熟悉 LangChain / LlamaIndex / Dify 等至少一种` hard parent is also retained. Different-evidence constraints remain independent.

`requirement-semantics-v42.77` adds one same-evidence repair for a second inline alternative/cardinality clause inside an already-grounded hard constraint. Only `skill must_have` children sharing the exact parent evidence are eligible; the parent must contain at least two comma-delimited clauses, and the child must be a strict atomic substring of a later clause that independently matches the existing bounded alternative/cardinality grammar. Such children are treated as non-blocking alternative children. This closes the live v42.76 shape where `LangChain`, `LlamaIndex`, and `Dify` were all emitted as hard requirements even though the JD says `熟悉 LangChain / LlamaIndex / Dify 等至少一种`. Later clauses without cardinality wording, different-evidence skills, explicit hard children, and the first clause remain unchanged.

`requirement-semantics-v42.78` adds one importance-only recovery for a Provider-softened experience fact whose own exact-grounded text is explicitly mandatory. The row must already be `experience preferred`, contain no soft/bonus marker, and match either the existing explicit experience grammar (`需要有/有/具备 ... 经验`) or that same fact followed by one negative-consequence segment such as `非...无法...`. Recognized requirements-section rows keep using the existing `requirement_section_default_must_have` repair so Trace behavior remains stable; v42.78 only covers the gap outside that path. Soft forms such as `有...经验者优先` remain preferred.

`requirement-semantics-v42.79` narrows one Provider-native type drift observed after v42.78 Case 5. A `must_have` row of the wrong type may be normalized to `experience` only when its first comma-delimited clause is an exact explicit experience fact (`有/具备 ... 经验`) and the second clause is a bounded explanatory qualifier tied to that experience, such as production-environment performance/stability knowledge or understanding the applicability/evaluation of different strategies. The full grounded text and evidence are preserved, capability is cleared, and independent compound hard requirements such as `有 Agent 项目经验,熟悉 Python 服务开发` are not converted.

`requirement-semantics-v42.80` removes one bounded duplicate-responsibility shape observed in the v42.79 initial Canary. A `responsibility must_have` row is dropped only when it is a short exact-grounded numbered scope label, contains no responsibility action verb or qualification/skill/experience/threshold marker, and at least three later grounded action-shaped responsibility rows prove that the detailed duties are already represented. This prevents short labels such as `企业级AI与智能体应用开发` from adding a second hard weight on top of the concrete duties. Short actionable responsibilities such as `负责AI平台开发`, explicit skills, and incomplete contexts remain unchanged.

`requirement-semantics-v42.81` closes two source-proven residual shapes from the v42.80 Canary without adding generic type or substring normalization. First, the existing bounded technical-skill-from-experience predicate is re-applied after split/dedupe repairs so a final exact-grounded hard clause such as `熟悉智能体(Agent)技术栈(如LangChain、AutoGPT等)` cannot remain routed as experience merely because an earlier mixed-scope repair produced it late in the pipeline. Second, same-evidence `education must_have` child dedupe no longer requires an explicit requirements-section heading when both parent and child are education rows, the child is a strict substring of the full education parent, and both share the exact evidence span. Cross-type subclauses outside a recognized requirements section remain untouched.

`requirement-semantics-v42.82` closes one source-proven missing hard cardinality shape from v42.81 Case 3. In a recognized requirements section, an omitted comma-delimited segment may be recovered as `constraint must_have` only when it begins with a bounded knowledge verb (`深入了解/理解/了解/熟悉/掌握/精通`), contains the existing explicit cardinality grammar such as `至少一个`, has no soft marker, and the exact segment is uniquely grounded in the JD. This recovers `深入了解至少一个大模型体系` without synthesizing ordinary neighboring clauses or promoting `...者优先` segments.

`requirement-semantics-v42.83` closes one same-source hard constraint duplicate observed in the v42.82 Case 3 live extraction. Inside a recognized requirements section, a `constraint must_have` strict child is removed only when an already-emitted hard parent constraint contains that exact child, both rows share the exact evidence span, neither is soft, and the parent has no alternative/cardinality grammar. This removes duplicate weighting such as `了解国外前沿产品网站和文档,具备国际化视角` when the full grounded requirement `有英文读写能力,了解国外前沿产品网站和文档,具备国际化视角` is already present. Cardinality parents are explicitly excluded so their dedicated repair strategies and trace semantics remain unchanged; different-evidence constraints are preserved.

`requirement-semantics-v42.84` closes a Provider-native same-evidence hard fan-out observed in v42.83 Case 4 without adding generic semantic grouping. In a recognized requirements section, an omitted inline alternative parent is recovered only when one exact evidence span already produced at least two `skill must_have` children from the same comma-delimited segment and that segment contains explicit `或` or bounded cardinality grammar such as `至少一种/至少一门`; the exact grounded segment is restored as `constraint must_have`, while only those same-evidence atomic children become preferred. Separately, an omitted experience umbrella such as `有向量数据库(Milvus / Weaviate / Pinecone / Qdrant 等)的实际使用经验` is restored only when the exact line is an explicit experience fact with a bounded parenthetical enumeration and at least two hard skill children came from that exact parenthesis; those example children are then removed. Recovered inline parents do not create global alternative scopes, so conjunctive hard siblings such as `具备良好的工程规范和代码品味` remain hard. Parenthetical enumeration removal is gated on the recovered experience parent and therefore does not collapse ordinary hard skill lists such as `熟悉推理引擎(vLLM / TGI / Triton 等)`.

`requirement-semantics-v42.85` closes one source-proven importance regression observed in v42.84 Case 3. When an exact same-evidence `skill must_have` clause immediately follows an already recovered hard alternative/cardinality parent, it is preserved as hard only if the bridge contains punctuation/whitespace only, the child itself is a complete bounded hard-skill clause (`熟悉/熟练/掌握/精通/使用...`), and neither the child nor shared evidence contains a soft marker. This keeps `熟悉基于DeepSeek的微调、训练、建立智能体应用` hard after `深入了解至少一个大模型体系` instead of letting the generic alternative-child scope downgrade it to preferred. True atomic alternative children remain governed by the existing alternative-child repair, and explicitly soft siblings remain preferred.

`requirement-semantics-v42.86` prevents the inline-cardinality recovery from duplicating a gate that is already represented inside one exact same-evidence hard constraint. Recovery is skipped only when an existing `constraint must_have` on that exact source line strictly contains the exact cardinality segment; exact standalone coverage still skips as before. This removes duplicate weighting when a Provider already emits the full hard clause `了解国内外主流大模型技术,深入了解至少一个大模型体系,熟悉基于DeepSeek...`, while preserving the v42.82 fail-safe when the Provider emits only the neighboring skills and truly omits `深入了解至少一个大模型体系`.

`requirement-semantics-v42.87` closes one source-proven omission observed in v42.86 Case 4 without adding generic clause synthesis. Inside a recognized requirements section, the exact hard sibling `具备良好的工程规范和代码品味` is recovered only when it is the second comma-delimited segment of the same uniquely grounded source line, the first segment is already represented by a same-evidence `constraint must_have` cardinality parent, and the line contains no soft marker. The recovered sibling remains `constraint must_have` and is explicitly protected from generic alternative-child softening. Different wording/evidence, already-covered siblings, and forms such as `具备良好的工程规范和代码品味者优先` are untouched.

`requirement-semantics-v42.88` closes one source-proven backend-component fan-out observed in v42.87 Case 4. The repair is intentionally exact-shape: within a recognized requirements section, the uniquely grounded source line must state `熟悉后端系统开发常用组件` and contain `MySQL/PostgreSQL`, `Redis`, and `消息队列`, with the next non-empty line exactly `(Kafka/RabbitMQ/RocketMQ)`. Only when at least two hard capability-decorated children have already fanned out from those slash groups are the MySQL/PostgreSQL, Redis, and Kafka/RabbitMQ/RocketMQ hard children removed and the grounded backend-component source line recovered as one `constraint must_have` with no capability annotation. Single-component hard skills, soft-marked lines, different source shapes, and different-evidence requirements are untouched.

`requirement-semantics-v42.89` keeps that exact backend-component source contract and only broadens child identity for the second live Provider shape. In addition to atomic child text, a hard `skill` child may be treated as fan-out when its `originalText` is the full exact backend-component source line (or the same line without the numeric prefix), its `evidenceSpan` is that same source line, and its capability is one of `MySQL / PostgreSQL / Redis / Kafka / RabbitMQ / RocketMQ`. This closes capability-decorated full-line duplication without introducing generic same-line or slash-list deduplication.

`requirement-semantics-v42.90` keeps the same exact source contract and adds only the third live Provider identity shape. The backend-component umbrella may also be recovered when exactly one hard `skill` is grounded to the full backend-component source line with normalized capability `熟悉后端系统开发常用组件` and exactly one hard `skill` is grounded to the immediately following `(Kafka/RabbitMQ/RocketMQ)` line with normalized capability `消息队列`. Both rows are removed and the exact backend-component source line is persisted once as `constraint must_have`. Either row alone, different evidence, soft wording, or unrelated backend skills do not trigger the repair.

`requirement-extractor-v42.91` is an evidence-rebuild cohort only and intentionally keeps `requirement-semantics-v42.90`, Prompt v7, `grounding-v1`, `requirement-coverage-v4`, and the existing retry policy unchanged. It exists because the stopped v42.90 live Trace was inconsistent with deterministic replay under the later committed stable HEAD; the new extractor version creates a fresh immutable live cohort without claiming a new semantic repair.

`requirement-semantics-v42.92` closes one source-proven duplicate-weighting gap from the v42.91 Case 4 bonus section. Inside a recognized explicit bonus section, capability-insensitive final deduplication is enabled only when multiple `bonus` Requirements have the same type, presentation-equivalent full-line `originalText`, the same presentation-equivalent `evidenceSpan`, and distinct non-empty normalized capabilities. This collapses Provider full-line fan-out such as one numbered `熟悉 vLLM / TGI / Triton Inference Server 等推理引擎` bonus bullet emitted three times with capabilities `vLLM / TGI / Triton`, or one `有 CUDA 编程或模型推理优化经验` bullet emitted twice with different capabilities. Distinct atomic `originalText`, different evidence, different type, non-bonus items, and non-bonus sections remain source-sensitive and are not merged.

`requirement-semantics-v42.93` closes the v42.92 Case 2 importance drift without changing explicit bonus semantics. A Provider-native `bonus` row is normalized to `preferred` only when its uniquely grounded standalone clause ends in explicit preferred wording (`优先` / `者优先` / `preferred`), is outside a recognized bonus section, has no explicit `加分` / `bonus` / `nice-to-have` wording, and matches the existing bounded preferred-suffix or preferred-alternative grammar. Inline bonus labels such as `加分项: ...` remain authoritative for Provider-native `bonus`. This aligns current live behavior with the long-standing v33 rule that standalone `...者优先` facts are preferred rather than hard requirements. The historical Eval v1 dataset remains immutable; Eval v2 updates the two standalone `者优先` expectations from `bonus` to `preferred` instead of rewriting v1 in place.

`requirement-semantics-v42.94` closes one Case 3 type drift exposed by the v42.93 bounded resume. A compound evaluative trait is normalized from `skill must_have` to `constraint must_have` whenever every comma-delimited segment matches the existing strict responsibility/pressure trait grammar (for example `具有高度的责任心,并具有较高的抗压能力`), regardless of whether the Provider's `normalizedCapability` is a literal substring of the Chinese source text. The grammar itself is unchanged, so concrete skills and arbitrary multi-clause abilities remain unaffected.

`requirement-semantics-v42.95` closes the residual mixed-importance backend-component fan-out observed after v42.94 Case 4 finally produced a reviewable extraction. The existing exact backend-component umbrella recovery remains unchanged and still requires hard fan-out proof from the uniquely grounded source shape. Once that exact `constraint must_have` umbrella is recovered, bounded component children for MySQL, PostgreSQL, Redis, Kafka, RabbitMQ, and RocketMQ are treated as redundant fan-out whether they remain hard or have already been softened to preferred by alternative normalization. Without successful exact umbrella recovery, preferred component skills remain untouched.

Starting with `requirement-extractor-v42.10`, structured-output failures are classified without logging raw Provider text: `structured_output_json`, `structured_output_validation`, `provider_response_shape`, `provider_refusal`, and transport/response JSON stages are distinguishable in the public error/Trace message. `requirement-extractor-v42.12` additionally permits one retry for `structured_output_json` only, inside the existing explicit `max_new_extractions` attempt budget. `requirement-extractor-v42.13` adds safe malformed-output diagnostics (`finishReason`, token usage, output character count, and requested completion-token cap) to distinguish truncation from Provider JSON drift without logging raw output. `requirement-extractor-v42.14` keeps that diagnostic behavior unchanged while adding `requirement-semantics-v42`; v42.15 through v42.27 keep the same Provider diagnostics and single-retry policy while adding their bounded semantic repairs. v42.28 kept `requirement-semantics-v42.27` and the same retry policy while simplifying only the Provider-facing schema; v42.29 restores the strict discriminated Provider schema; v42.30 keeps that Provider contract and retry budget while adding only the two bounded deterministic semantic repairs above; v42.31 keeps `requirement-semantics-v42.30` and changes only responsibility coverage auditing as described below; v42.32 keeps `requirement-coverage-v3` and adds only the bounded alternative-direction experience-type drift repair above; v42.33 keeps `requirement-coverage-v3` and adds only the explicit bonus-section importance inheritance above; v42.34 keeps both of those protections and extends only the bounded direction sibling proof to non-hard constraint drift; v42.35 keeps those protections and adds only terminal-punctuation-equivalent full-line sibling proof; v42.36 keeps all prior contracts and adds only the split-compound abstract-evaluative first-segment normalization above; v42.37 adds only exact responsibility parent/subclause deduplication; v42.38 adds only bounded technical-skill-from-experience type recovery; v42.39 adds only the full-phrase umbrella example-capability normalization above; v42.40 adds only the bounded multi-clause evaluative-trait normalization above; v42.41 adds only experience-exact-duplicate capability-insensitive identity plus presentation-equivalent umbrella full-phrase comparison; v42.42 adds only the bounded postfix one-of cardinality grammar; v42.43 further makes experience duplicate identity evidence-span-insensitive for identical facts while leaving all other types source-sensitive; v42.44 adds only the exact-grounded `包括但不限于` responsibility-enumeration collapse described above; v42.45 adds only bounded ordinal requirement-section recognition plus standalone evaluative-trait normalization; v42.46 adds only the exact hard technical-stack constraint-drift recovery described above; v42.47 adds only the same-evidence `包括但不限于` example-fragment deduplication described above; v42.48 adds only the bounded same-evidence `,如/例如/比如` example-fragment deduplication plus umbrella capability recovery described above; v42.49 adds only the bounded core-technology compound experience-to-constraint recovery described above; v42.50 adds only explicit-soft preferred same-source capability-fanout deduplication while preserving unsoftened preferred, hard, and bonus multi-capability facts; v42.51 adds only same-child capability-fanout deduplication inside one bounded cardinality scope; v42.52 adds only bounded numbered/annotated bonus-section heading recognition; v42.53 adds only source-proven trailing-soft-fragment cleanup for hard umbrella capabilities; v42.54 adds only bare-numbered sibling boundary recovery for cardinality scopes; v42.55 keeps `requirement-semantics-v42.54` and advances coverage to `requirement-coverage-v4` with only the explicit numbered bonus-section severe-omission gate described below; v42.56 keeps `requirement-coverage-v4` and adds only the bounded engineering-habit quality-fanout collapse described above; v42.57 keeps that coverage contract and adds only the hard `包括但不限于` umbrella capability-fanout dedupe described above; v42.58 adds only the bounded conceptual-mechanism `domain` type recovery described above; v42.59 adds only the explicit-requirement-section education importance recovery described above; v42.60 adds only exact same-source education capability-fanout deduplication described above; v42.61 adds only capability-optional recovery for the already-bounded conceptual-mechanism `domain` drift described above; v42.62 adds only bounded alternative-direction source-identity normalization described above; v42.63 changes only that normalization's body equality to presentation-equivalent terminal-punctuation matching; v42.64 only broadens the already-bounded hard example-capability collapse to explicit parenthetical slash/enumeration lists ending in `等`/`etc.`; v42.65 adds only the exact-source `有/具备 ... 经验, 能/能够 ...` canonical experience dedupe described above; v42.66 adds only the bounded numbered cardinality-group-header restoration described above; v42.67 adds only bounded inline-parenthetical example-child removal under an exact same-evidence hard umbrella; v42.68 only extends the existing exact cardinality recovery to bracketed requirement headings and same-source skill subfragments; v42.69 keeps the same semantic and coverage policies while raising only the Provider raw requirement capacity to 64, with the final validated business limit still fixed at 50; v42.70 adds only the bounded line-start bare-numbered cardinality sibling boundary recovery described above; v42.71 adds only the same-evidence leading-cardinality-subclause dedupe described above; v42.72 adds only the bounded imperative application-material hard-importance recovery described above; v42.73 adds only the bounded presentation-equivalent responsibility-parent and same-evidence education-subclause dedupe described above; v42.74 adds only the bounded non-experience semantic `experience` subclause dedupe described above; v42.75 adds only the explicit-requirements-section experience-threshold hard-importance recovery described above; v42.76 extends only the existing leading-cardinality-subclause dedupe to same-evidence hard constraint children described above; v42.77 adds only the same-evidence secondary-inline-cardinality child importance repair described above; v42.78 adds only the explicit-mandatory experience importance recovery described above; v42.79 adds only the bounded explicit-experience-plus-explanatory-qualifier type recovery described above; v42.80 adds only the bounded redundant short scope-summary responsibility removal described above; v42.81 adds only the post-repair technical-skill type recovery and no-heading same-evidence education-child dedupe described above; v42.82 adds only the bounded exact-grounded inline hard-cardinality recovery described above; v42.83 adds only the same-evidence non-cardinality hard constraint subclause dedupe described above; v42.84 adds only the bounded same-evidence inline-alternative parent recovery plus explicit-experience example-fanout recovery described above; v42.85 adds only the bounded post-cardinality hard-sibling preservation described above; v42.86 adds only same-evidence compound-constraint coverage recognition for inline cardinality recovery; v42.87 adds only the bounded same-line post-cardinality engineering-practice recovery described above; v42.88 adds only the exact-shape backend-component capability-fanout collapse described above; v42.89 only extends that same repair to full-line capability-decorated child identity; v42.90 only extends that exact repair to the live umbrella-plus-queue-category pair described above; v42.91 keeps `requirement-semantics-v42.90` unchanged and advances only the extractor cohort so live evidence can be regenerated against the current committed HEAD; v42.92 adds only the explicit-bonus-section same-full-line capability-fanout dedupe described above; v42.93 adds only standalone explicit-preferred false-bonus normalization; v42.94 only removes the unnecessary literal-capability dependency from the already-bounded compound evaluative-trait constraint repair described above; v42.95 only extends the exact backend-component fan-out cleanup to residual preferred children after umbrella recovery. The retry is never applied to field validation or response-shape contract errors, and a second malformed JSON result fails normally.

`requirement-extraction-v7` adds an explicit coverage contract: job responsibilities/duties/work content are first-class `JobRequirement` facts alongside qualifications, and materially distinct duty lines should be emitted as `responsibility` when they can be anchored to one source candidate. This addresses the frozen v39 Canary where the Provider returned qualifications but omitted most core duties even though every duty line was present in the source-candidate input.

`requirement-coverage-v4` keeps the v3 responsibility gate unchanged and adds one second fail-closed check for an explicit bonus section. If a recognized bonus heading contains at least four numbered bonus candidates, fewer than two represented candidates is treated as a severe omission and the extraction fails instead of being accepted as technically successful. This is intentionally coarse: it does not synthesize the missing bonus facts, does not require perfect bonus recall, and does not run for small bonus sections. The responsibility gate still treats a short numbered line as a structural group heading only when the next non-empty line is a labeled concrete duty (`标签: 设计/构建/集成/开发/优化/...`) and still fails only when at least four duty candidates exist but fewer than two are represented. Trace records the responsibility audit separately; bonus severe-omission failures are visible in the extraction error:

```json
{
  "coveragePolicyVersion": "requirement-coverage-v4",
  "coverageAudit": {
    "dutyCandidateCount": 6,
    "coveredDutyCount": 2,
    "minimumCoveredDutyCount": 2,
    "enforced": true
  }
}
```

The coverage gate never synthesizes missing Requirements and never uses fuzzy/semantic evidence matching; it only turns a severe omission into an observable extraction failure instead of a false technical success.

## Scope exclusions

This slice does not implement:

- Eligibility;
- Profile-to-Requirement Match;
- batch Ranking;
- Requirement human review/acceptance;
- OCR or job-page fetching;
- Agent orchestration;
- live model quality approval without credentials.
